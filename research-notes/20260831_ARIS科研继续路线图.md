# 科研继续路线图（ARIS 方法论 × 项目现状，2026-08-31）

> 依据：`20260826_总体设计精简版.md`（主线定稿）、`20260827_E1/E2a 执行结果`、
> ARIS 证据预检（51/53 verified）与 result-to-claim 评审包。
> 原则：先修数据口径 → 补实验矩阵缺口 → 外部裁决 claim → 论文回填与审计。

## P0 立即修复（半天内，纯机械）

1. **oneshot 中位数口径统一为 TERMINAL-only（97.0）**，三处：
   - `research/20260827_E1执行结果_hero对比.md` 成绩单（97.5→97.0）；
   - `论文_反事实模拟/article_counterfactual_v4_cn.tex` L726（表）与 L740（诚实边界段）；
   - caption 已写"TERMINAL 口径"，数字必须与之自洽。修复对结论有利无害（96.5 vs 97.0）。
2. **fig11/12/14 用 v0b 合并 manifest 重跑**（E1 报告遗留 TODO），使图含 v0b 列
   与论文表 `tab:e1-main` 同源。
3. 把证据预检脚本挂进实验流程：`../.aris/verify_e1_e2a_claims.py`
   （以后每个 E 报告生成时同步跑一次，防止数字漂移）。

## P1 补实验矩阵缺口（设计文档已列，1–2 周）

| 实验 | 目的 | 状态 |
|---|---|---|
| E3 λ/τ 敏感性 | LCB 门控参数稳健性（τ 太松/太紧的退化曲线） | 未跑 |
| E4 穷举校验 | 冻结候选集上 Racing 穷举值 vs hero 排序一致性（小子集） | 未跑 |
| T3/M-D1 切换成本显式计价 | G = Δ − λ·C_switch 完整化；**用 E2a 数据校准 disruption_steps**（常数 2 已被证伪，量级是数十步） | 未跑 |
| T5/M-D2 闭环评估 | §3 全实验矩阵一次成型（论文主表） | 未跑 |

E3/E4 直接决定 C3（及格线 claim）能否从 partial 升 yes——这是外部评审
最可能攻击的点（中位持平、胜率 14/71）。

## P2 外部裁决（ARIS result-to-claim 门）

- 把 `20260831_result-to-claim_评审包.md` §4 提示词粘贴给任一强外部模型，
  回填 §5；在此之前一切 claim 按 `REVIEW_UNAVAILABLE` 处理，不写进论文结论句。
- 有了裁决后按 §6 路由：confirm → 论文；partial → 补实验；no → 收窄叙事。

## P3 论文线（与 P1 并行推进）

1. v4 主稿回填：E1 全表（含修复口径）、E2a 因果表 T4/F3、E3/E4 新结果；
   hero_v0 输给 oneshot 的诚实段落保留（这是叙事资产不是负债）。
2. ARIS 审计栈三连（本地可跑部分）：
   - `/paper-claim-audit` 精神 = 本文的证据预检脚本扩展到论文每个数字；
   - `/citation-audit`：v4 bib 逐条验证存在性/元数据/上下文恰当性；
   - `/kill-argument`（需外部模型）：投稿前最强拒稿信 + 反驳演练。

## P4 完全体（投稿后/下一篇）

- V1 开放组合空间（FJSP × assigner × MAPF × 参数档，manifest capabilities 掩码）
  ——"班子不固定"的完全体，V0 冻结池口径分开报告不混表；
- future work 队列已冻结于设计文档 §6（expert iteration round≥2、judgement 模块、
  学习线特征层、per-decision oracle 标签），不回头。

## ARIS 使用备忘（本项目内）

- skills 已装：`260601天工论文准备/.claude/skills/`（49 个，symlink →
  `flex_manufacture/aris_repo`）；更新 = `cd aris_repo && git pull`。
- 常用入口：Claude Code 里 `/result-to-claim`、`/paper-claim-audit`、
  `/citation-audit`、`/ablation-planner`、`/paper-writing`；
  跨模型评审需 Codex MCP，无则全部走 manual 评审包模式（本文示范）。
- ARIS 纪律三条：① claim 裁决必须外部模型做；② 单一数据集正结果不支撑
  泛化 claim；③ 评审不可用时 fail-closed（REVIEW_UNAVAILABLE），agent 不自判。
